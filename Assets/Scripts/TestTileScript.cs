using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;

public class TestTileScript : MonoBehaviour
{
    public Tilemap testTileMap;

	public TileDictionaryScript TileDictionary;

	//public NormalTile normalTile;
    //public Player selectedUnit;

    //public int i = 0;
    // Update is called once per frame
    /*void Update()
    {
        if (Input.GetKeyDown("space"))
        {
            Debug.Log("Do something");
            this.GetComponent<Tilemap>().SetTile(new Vector3Int(i,i,0), TileDictionary.GetRandomGrassTile());
            i++;
        }

        // left click - get info from selected tile
        if (Input.GetMouseButtonUp(0))
        {
            
		}
    }*/

}
